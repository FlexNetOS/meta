# Phase 1.5 Release Infrastructure, GitKB Setup, and Key Bootstrap Proof — 2026-06-28

## Verdict

Phase 2 overlays remain **blocked**. Phase 1.5 exposed and partially repaired the release/setup layer that must exist before overlay work can be trusted.

Confirmed upgrades/proofs in this chunk:

- The three overlooked upstream release repositories were cloned and inspected under the clean-room recovery area:
  - `/home/drdave/Desktop/meta-cleanroom-recovery/cleanrooms/phase1-5-release-infra-20260628/homebrew-tap`
  - `/home/drdave/Desktop/meta-cleanroom-recovery/cleanrooms/phase1-5-release-infra-20260628/gitkb-releases`
  - `/home/drdave/Desktop/meta-cleanroom-recovery/cleanrooms/phase1-5-release-infra-20260628/gitkb-desktop-releases`
- GitKB public docs were mined for the actual setup contract: `git-kb init`, `git-kb init codex`, MCP setup, and meta-repo discovery via `.kb/config.toml`.
- The active repo's `.kb/config.toml` now has the documented meta-repo discovery setting:
  ```toml
  [repos]
  strategy = "meta"
  ```
- `git kb doctor --json` now confirms `repos.config = ok`, `repos.discovery = ok`, and `repos.remotes = ok`; remaining warnings are projection/workspace-checkout repair work plus alias collisions from multiple entries that share `FlexNetOS/meta`.
- Envctl key request was filed as <https://github.com/FlexNetOS/envctl/issues/405>.
- GitHub Actions now installs the Linux `wild-linker` binary alongside the `kache` rustc wrapper before Cargo runs, preserving the strict-upgrade `.cargo/config.toml` wild/kache settings instead of deleting them to get CI green.

Confirmed blockers/gaps and setup findings from Phase 1.5:

- `FlexNetOS/homebrew-tap`, `FlexNetOS/gitkb-releases`, and `FlexNetOS/gitkb-desktop-releases` do not currently resolve through `gh repo view`; they are missing or inaccessible from this authenticated environment.
- `git kb init` cannot be re-run on this root without deleting `.kb/`; it correctly reports the KB is already initialized. Deleting/reinitializing `.kb/` would be destructive and is not allowed.
- `git-kb init codex` has now been run for Phase 1.5 and generated the required GitKB/Codex skills plus instruction assets under `.kb/skills/`, `.codex/skills/`, and `.codex/instructions/`.
- Raw `meta exec init` originally resolved to system `/usr/sbin/init` across repos and failed with `Explicit --user argument required to run as user manager`; Phase 1.5 now ships a repo-owned `scripts/recovery/phase1-5/init` shim so `meta exec init` runs the intended GitKB/Codex init gate instead of systemd.
- Node/npm/NAPI remains a key-and-plan lane only. The clean-room `gitkb/meta` evidence has no root `package.json`, no `napi/` package, and no implemented NAPI bridge.

## Source truth vs generated output vs release artifacts

| Layer | Source truth | Generated output | Release artifact |
| --- | --- | --- | --- |
| GitKB docs/setup | `https://gitkb.com/docs/`, `.kb/AGENTS.md`, `.kb/config.toml`, `.kb/store/**` | `.kb/gitkb.db`, `.kb/cache/**`, `.kb/workspace/**`, code/projection indexes | GitKB sync payloads and pulled sparse document sets |
| Meta clean-room source | `gitkb/meta` clean-room clone, `Cargo.toml`, child crate manifests, `install.sh`, `install.ps1`, `.github/workflows/{ci,release}.yml` | `target/**`, generated checksums, workflow build outputs | `meta-*.tar.gz`, `meta-windows-x64.zip`, `*.sha256`, `checksums.txt` |
| GitKB CLI public release | `gitkb/gitkb-releases` `install.sh`, `on-child-release.yml`, `test-install.yml` | downloaded private-source assets under workflow `release/` and `checksums/` | public GitHub Releases in `gitkb/gitkb-releases` and future `FlexNetOS/gitkb-releases` |
| Homebrew tap | `gitkb/homebrew-tap` formula files | generated formula update branch/PR | merged formula rows under `Formula/*.rb` |
| Desktop public release | `gitkb/gitkb-desktop-releases` `on-child-release.yml` | downloaded/verified `GitKB_universal.dmg` bundle | public GitHub Releases in desktop release mirror |
| Envctl secrets | `envctl` docs and Rust sources for `secretctl`/`secretd` | short-lived minted GitHub installation tokens | no long-lived artifact; only scoped JSON `{token, expires_at_unix}` exits the broker |
| NPM/NAPI | future FlexNetOS source package/workflow | Node build outputs, native bindings | npm package published with `NPM_TOKEN`/`NODE_AUTH_TOKEN` |

## Overlooked upstream release repos now inspected

### `gitkb/homebrew-tap`

Clone evidence:

- HEAD: `c27bda1573537affa49bfec15fc7b3b6a537e0b3`
- Current files: `Formula/atc.rb`, `Formula/gitkb.rb`, `Formula/meta-cli.rb`
- `Formula/gitkb.rb` version: `0.2.12`
- `Formula/meta-cli.rb` version: `0.2.22`
- `Formula/atc.rb` version: `0.1.8`

Contract:

- Formula URLs point at GitHub Release assets by OS/arch.
- `gitkb.rb` installs the `git-kb` binary and tests `git-kb --version`.
- `meta-cli.rb` installs `meta`, `meta-git`, `meta-project`, `meta-mcp`, and `loop`, then tests `meta --version`.
- A FlexNetOS mirror must rewrite homepage/release URLs from `gitkb/*` to the final FlexNetOS release repos only after those repos exist and release assets are present.

### `gitkb/gitkb-releases`

Clone evidence:

- HEAD: `dcd8a4d6f4573e2c2ad44dd84adf198ce9718c27`
- Latest public release observed with `gh release list`: `v0.2.12`
- Key files: `README.md`, `install.sh`, `.github/workflows/on-child-release.yml`, `.github/workflows/test-install.yml`

Contract:

- Public release mirror is triggered by `repository_dispatch` type `child-release-created`.
- It downloads source assets from private `gitkb/gitkb-core` using `CORE_REPO_PAT`.
- It republishes `*.tar.gz`, `*.sha256`, and `checksums.txt` through `softprops/action-gh-release`.
- It optionally posts Discord release notifications through `DISCORD_RELEASES_WEBHOOK_URL`.
- It updates `gitkb/homebrew-tap` using `HOMEBREW_TAP_TOKEN` by cloning the tap, writing `Formula/gitkb.rb`, opening a PR, and attempting auto-merge.
- `install.sh` downloads release assets, downloads `.sha256`, verifies checksum, installs to `${INSTALL_DIR:-$HOME/.local/bin}`, and verifies the binary runs.
- `test-install.yml` verifies install script behavior on Linux and macOS.

FlexNetOS adaptation gap:

- `REPO="gitkb/gitkb-releases"`, source download repo `gitkb/gitkb-core`, and Homebrew tap `gitkb/homebrew-tap` must become FlexNetOS-owned equivalents.
- The source-release token must be brokered through envctl if the source repo is private.

### `gitkb/gitkb-desktop-releases`

Clone evidence:

- HEAD: `c9f85ce688acdc9ae266ac58c9338dae56de6346`
- Latest public release observed with `gh release list`: `gitkb-desktop-v0.6.0`
- Key files: `README.md`, `.github/workflows/on-child-release.yml`

Contract:

- Public desktop release mirror is triggered by `repository_dispatch` type `child-release-created`.
- It accepts `v<semver>` or `gitkb-desktop-v<semver>` tags.
- It downloads private desktop assets from `gitkb/harmony-desktop` using `DESKTOP_SOURCE_REPO_PAT`.
- Expected assets are `GitKB_universal.dmg`, `GitKB_universal.dmg.sha256`, and `checksums.txt`.
- It verifies the DMG checksum before creating the public release.

FlexNetOS adaptation gap:

- The private desktop source repo and public mirror repo must be identified or created in FlexNetOS.
- The DMG signing/notarization status is not proven by this mirror. This repo only verifies checksums and republishes assets.

## GitKB docs/setup evidence

Docs mined:

- <https://gitkb.com/docs/>
- <https://gitkb.com/docs/getting-started/installation/>
- <https://gitkb.com/docs/getting-started/quick-start/>
- <https://gitkb.com/docs/getting-started/codex/>
- <https://gitkb.com/docs/getting-started/mcp-setup/>
- <https://gitkb.com/docs/reference/configuration/>
- <https://gitkb.com/docs/reference/agent-harnesses/>
- <https://gitkb.com/docs/cli-reference/>

Confirmed setup contract:

1. `git-kb init` creates the `.kb/` project root state.
2. `git-kb init codex` scaffolds Codex skills/instructions and symlinks/adapter assets.
3. Codex MCP setup is `codex mcp add gitkb -- git-kb mcp`, or the equivalent `[mcp_servers.gitkb]` config.
4. For multi-repo workspaces, `.kb/config.toml` should use:
   ```toml
   [repos]
   strategy = "meta"
   ```
5. GitKB code intelligence can run before full KB initialization, but full agent context and task traceability require the KB and harness setup.

Local command evidence:

```text
$ git-kb --version
git-kb 0.2.10

$ meta --version
meta 0.2.25

$ git-kb init --no-verify --name "FlexNetOS Meta"
error: Knowledge base already initialized
hint: Remove .kb/ directory to reinitialize

$ git-kb init codex
Codex init summary:
  created .kb/skills/...
  created .codex/skills/...
  created .codex/instructions/codex-rules.md
  created .codex/instructions/gitkb-process.md
53 created

$ PHASE15_META_ROOT=/home/drdave/Desktop/meta PATH="scripts/recovery/phase1-5:$PATH" meta exec init
✓ . (meta):
phase1.5 init: git-kb init already initialized; treating as idempotent success
Codex init summary: ... 53 skipped
✓ child repos:
phase1.5 init: skip non-root repo ...
72 commands complete
```

Phase 1.5 also fixed the meta workspace setup precondition that blocked `meta exec init`: four configured repos (`FlexNetOS/assets`, `FlexNetOS/flexnetos_brain`, `FlexNetOS/flexnetos_wiki`, and `FlexNetOS/my-wiki`) did not exist or were inaccessible. They were created in the FlexNetOS org and cloned into the active meta workspace, after which `rtk meta project check` reported `All projects are cloned and present.`

After adding `[repos] strategy = "meta"`:

```text
repos.config    ok    [repos] section configured in .kb/config.toml
repos.discovery ok    73 repos discovered (strategy: meta)
repos.remotes   ok    All repos have remote URLs configured
```

Remaining GitKB warnings are not fixed in this chunk:

- `repos.aliases` warns that `claude-plugin`, `copilot-plugin`, and `meta` all claim aliases for `FlexNetOS/meta` / `meta`.
- `kb.projection` warns about repairable SQLite projection drift.
- `kb.workspace_checkouts` warns about stale workspace checkout baselines.

These are Phase 1.5 setup follow-ups; they are not Phase 2 overlay work.

## CI strict-upgrade companion for envctl wild/kache

The PR initially surfaced a real CI substrate gap after the `kache` wrapper was installed: the `envctl (meta-tree build)` job reached Cargo and then failed because Linux CI did not have the `wild` linker binary required by `.cargo/config.toml`:

```text
clang: error: invalid linker name in argument '--ld-path=wild'
```

That config is intentional envctl upgrade state:

```toml
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-Clink-arg=--ld-path=wild"]

[build]
rustc-wrapper = "kache"
```

The fix is therefore to provision the missing companion tool, not to remove the config. `.github/workflows/ci.yml` now installs `wild-linker` `0.9.0` on Linux runners before installing/verifying `kache` `0.6.0`; macOS/Windows skip the Linux-only linker install. Local smoke evidence:

```text
$ RUNNER_OS=Linux ... bash -c '<workflow tool-wrapper snippet>'
/home/drdave/.local/bin/wild
kache 0.6.0
```

## Phase 1.5 init-gate completion

Phase 1.5 is not just a report anymore; the setup gate was run and repaired:

1. `git-kb init` was run and proved the root KB already exists. The Phase 1.5 shim treats this exact existing-KB condition as idempotent success and still fails closed on any other init error.
2. `git-kb init codex` was run and generated the Codex/GitKB skills and instructions that were previously only listed in dry-run output.
3. Raw `meta exec init` was run, failed by dispatching system `/usr/sbin/init`, and was fixed by adding `scripts/recovery/phase1-5/init` to the command path.
4. `meta exec init` was rerun through the shim and completed all configured repos successfully: root ran GitKB/Codex init; child repos skipped as non-root.
5. Missing configured repos surfaced by `meta project check` were created and cloned before the successful `meta exec init` gate.

The shim is intentionally scoped to Phase 1.5 recovery. It is evidence and a safe init gate, not a general replacement for the `meta` command surface.

## Envctl key request and mint contract

Filed issue: <https://github.com/FlexNetOS/envctl/issues/405>

Known available per owner:

- `CARGO_REGISTRY_TOKEN` for crates.io.
- `NPM_TOKEN` / `NODE_AUTH_TOKEN` for Node/npm/NAPI.

Still needed or needs confirmation:

| Secret / token | Purpose | Minimum scope |
| --- | --- | --- |
| `HOMEBREW_TAP_TOKEN` | Push formula branch/PRs into `FlexNetOS/homebrew-tap` | `contents:write`, `pull_requests:write`, metadata read on the tap |
| `ORG_REPO_BOOTSTRAP_TOKEN` or GitHub App installation token | Create/configure the three FlexNetOS release repos | one-time admin/bootstrap, then retire |
| `CORE_REPO_PAT` / FlexNetOS core source-release token | Download CLI release assets from private source if private | read releases/assets only |
| `DESKTOP_SOURCE_REPO_PAT` | Download Desktop DMG assets from private source if private | read releases/assets only |
| GitHub App app id/private key/installation id | Envctl short-lived repo-scoped token minting | broker-only in envctl vault |
| `DISCORD_RELEASES_WEBHOOK_URL` | Optional release notifications | optional webhook only |

Envctl's confirmed consumer shape:

```bash
secretctl mint-github \
  --installation-id "$INSTALLATION_ID" \
  --repository-ids "$REPO_IDS" \
  --permissions contents:write,metadata:read,pull_requests:write,actions:read \
  --ttl-secs 3600 \
  --output json
```

Expected stdout shape:

```json
{"token":"<scoped>","expires_at_unix":1234567890}
```

Do not paste secrets into issues, PRs, docs, chat, or logs. Store broker-only values in envctl/secretd and hand out only short-lived scoped tokens.

## Dependency/contract map gating Phase 2

```text
gitkb.com docs
  -> git-kb init / Codex / MCP / [repos] strategy=meta
  -> GitKB can discover the meta workspace and preserve context truth

upstream gitkb release repos
  -> homebrew formula + release mirror + desktop mirror contracts
  -> FlexNetOS mirror repos must exist and be configured
  -> envctl must broker scoped GitHub/App/source tokens
  -> release workflows can publish checksummed assets/formulas

crates.io key + Cargo manifests
  -> cargo publish lane can be wired/proven

npm/node key + future NAPI package
  -> only key availability proven; implementation still absent

only after these contracts are accepted
  -> Phase 2 overlay rows may modify FlexNetOS-specific source/config without guessing release substrate
```

## Phase 2 rows affected

Unblocked after owner accepts this Phase 1.5 evidence and the FlexNetOS release repos/key substrate are created:

- GitHub CI/release-substrate rows: evidence is ready, but repos/secrets are still missing.
- crates.io packaging rows: key availability is known, but publish ownership and crate naming still need registry-side verification at publish time.
- Homebrew rows: upstream formula/release automation contract is proven, but FlexNetOS tap repo/token are missing.
- GitKB/Codex setup rows: `.kb/config.toml` meta strategy is fixed, `git-kb init codex` has generated the Codex skills/instructions, and `meta exec init` now passes through the Phase 1.5 init shim.

Still blocked:

- NAPI/Node implementation rows: key exists, but no root package/NAPI source exists in clean-room meta.
- Desktop release rows: upstream mirror contract is proven, but FlexNetOS source/mirror repos and `DESKTOP_SOURCE_REPO_PAT` are missing.
- Any Phase 2 overlay implementation: still blocked until Phase 1.5 follow-ups are accepted or completed.

## Next Phase 1.5 follow-ups before Phase 2

1. Create or make accessible:
   - `FlexNetOS/homebrew-tap`
   - `FlexNetOS/gitkb-releases`
   - `FlexNetOS/gitkb-desktop-releases`
2. Mirror/adapt the upstream release repos with provenance:
   - upstream remote URL
   - upstream HEAD SHA
   - release tags mirrored
   - changed owner/repo strings
   - secrets required per workflow
3. Repair or explicitly defer GitKB projection/workspace-checkout warnings.
4. Resolve or document the `repos.aliases` collisions caused by `claude-plugin`, `copilot-plugin`, and root `meta` sharing the same `FlexNetOS/meta` remote.
5. Verify registry ownership immediately before publishing crates/npm packages.
